<?php
function f(): never
{
    $a = $_GET;
    $c = strlen(...);
    $b = match ($a["b"] + 1) {
        1 => $o?->f(),
        2 => htmlspecialchars($string, double_encode: false),
        3 => $c("a"),
    };
    match ($a + 1) {
        2 => print(1),
    };
    exit(1);
}
f();
